#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int n, fl = 0;
        cin >> n;
        int d[n], a[n];
        for (int i = 0; i < n; i++)
        {
            cin >> d[i];
        }
        
    }
    return 0;
}